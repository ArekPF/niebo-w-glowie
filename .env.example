# ZeptoMail Send Mail Token
# Find it in ZeptoMail dashboard → Mail Agents → SMTP & API Info → API tab
# Provide just the raw token – the application adds the "Zoho-enczapikey " prefix automatically.
ZEPTOMAIL_TOKEN=your_send_mail_token_here
